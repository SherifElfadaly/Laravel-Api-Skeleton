<location /socket>
    RewriteEngine On
    RewriteCond %{QUERY_STRING} transport=websocket    [NC]
    RewriteRule /(.*)           ws://skeleton_echo:6001/socket.io/%{QUERY_STRING} [P,L]
    ProxyPass http://skeleton_echo:6001
    ProxyPassReverse http://skeleton_echo:6001
</location>